# Place your TLS certificate files here:
#   fullchain.pem   — certificate + chain
#   privkey.pem     — private key
#
# These files are listed in .gitignore and must NOT be committed.
# Obtain them with Let's Encrypt:
#   sudo certbot certonly --standalone -d n8n.example.com
#   sudo cp /etc/letsencrypt/live/n8n.example.com/fullchain.pem ./
#   sudo cp /etc/letsencrypt/live/n8n.example.com/privkey.pem   ./
